[tool.ruff]
line-length = 88
target-version = "py311"
exclude = [
    ".git",
    "__pycache__",
    ".venv",
    "venv",
    ".pytest_cache",
    "htmlcov",
    "build",
    "dist",
    "*.egg-info",
    "alembic/versions",
]

[tool.ruff.lint]
# E/W (pycodestyle), F (pyflakes = flake8 + autoflake), I (isort)
select = ["E", "W", "F", "I"]
ignore = ["E203", "E501"]  # E501 is handled by the formatter; E203 conflicts with format

[tool.ruff.lint.isort]
known-first-party = ["src", "config"]

[tool.ruff.format]
# black-equivalent style (default)

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
markers = [
    "unit: unit test without DB (mock/pure functions); does not require PostgreSQL",
    "integration: integration test against real PostgreSQL",
    "slow: benchmark-grade test (full search budget); exclude with -m 'not slow' for the fast loop",
    "benchmark: parity vs the commercial program; the exact board counts are a commercial claim, only meaningful for the build that ships (see tests/unit/test_cutting_search.py). Excluded from CI because it is slow (~2 min); run it against the production build with `make benchmark`",
]
addopts = [
    "-v",
    "--tb=short",
    "--strict-markers",
    "--disable-warnings",
    "--cov=src",
    "--cov-report=term-missing",
    "--cov-report=html",
    "--cov-fail-under=80"
]

[tool.coverage.run]
source = ["src"]
omit = [
    "*/tests/*",
    "*/venv/*",
    "*/__pycache__/*",
    "*/migrations/*"
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError"
]