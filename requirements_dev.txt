-r requirements.txt

ruff==0.8.4
pytest==8.3.4
pytest-cov==4.1.0
pytest-mock==3.14.0
httpx==0.27.2

# Builds the native packing kernel in rust/ (`make rust`). Not a runtime
# dependency: the Dockerfile compiles the wheel in its own stage and installs
# only the result, and the suite falls back to the pure-Python geometry when
# the wheel is absent.
maturin==1.7.8
